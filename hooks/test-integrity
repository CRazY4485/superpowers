#!/usr/bin/env bash
# Thin wrapper: the tests check lives in hooks/commit-gates with the other
# commit-time checks, so a Bash call pays one process instead of three. This
# entry point exists for the check's own test suite and for running it alone.
exec bash "$(cd "$(dirname "$0")" && pwd)/commit-gates" --only tests
