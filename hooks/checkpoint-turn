#!/usr/bin/env bash
# Stop hook: snapshot the working tree once per turn, silently.
set -uo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
bash "${SCRIPT_DIR}/git-checkpoint" snapshot turn >/dev/null 2>&1 || true
exit 0
